# SPDX-FileCopyrightText: 2022 Zextras <https://www.zextras.com>
#
# SPDX-License-Identifier: AGPL-3.0-only

mutation keepVersions($node_id: ID!, $versions: [Int!]!, $keep_forever: Boolean!) {
    keepVersions(node_id: $node_id, versions: $versions, keep_forever: $keep_forever)
}
